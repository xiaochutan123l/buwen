# 步纹 (Buwen) - 产品需求文档 (PRD)

---

## 📋 文档目录

1. [产品概述](#1-产品概述)
2. [核心功能需求](#2-核心功能需求)
3. [界面布局与设计](#3-界面布局与设计)
4. [技术实现指南](#4-技术实现指南)
5. [视觉设计规范](#5-视觉设计规范)
6. [用户体验要点](#6-用户体验要点)
7. [AI Agent实现提示](#7-ai-agent实现提示)

---

## 1. 产品概述

### 1.1 产品定位

**步纹**是一个反焦虑、强调"直觉"和"心流"的个人项目**web端**管理工具。它不是传统的待办事项清单或日程管理工具，而是一个**"项目库"**系统，让用户可以深度规划多层级项目结构，但执行时完全自由、无压力、随心所欲。最亮点的两个点，一个是重项目，用户可以把复杂的项目层层拆分，每天可以根据心情，时间富裕程度来感性的选择某个项目中的任务，另一个亮点就是有个核心的项目库试图，所有项目一览无余的战士在用户面前，找到中意的项目点开找到待做的下一步任务直接拖拽到下方的日历日期格子里面，简单粗暴

### 1.2 核心理念

- **深度规划 + 自由执行**：支持多层级的项目拆分，但不强制时间安排
- **凭直觉选择**：每天根据心情和状态，从项目库中自由选择想做的任务
- **粗粒度安排**：只安排到天，不精确到小时和分钟
- **无deadline压力**：没有截止日期、没有提醒、没有外部push，没做完就没做完，不想做了直接删除
- **长期项目库**：项目持续存在，慢慢推进，不追求"清空待办"

### 1.3 典型使用场景

**用户画像**：有多个长期个人项目的工作者，希望细致规划但不想被时间表束缚

**典型工作流**：
1. 整理期（上帝视角）：通过左侧列表或项目详情，将大项目层层拆解为子任务。
2. 选择期（每日早晨）：浏览中心的项目库卡片，凭直觉挑选今天要做的任务。
3. 执行期（心流状态）：将任务拖拽至下方日历，专注当下，做完即止。
4. 调整期（随时）：不想做了直接从日历移除（任务退回项目库），无心理负担。

---

## 2. 核心功能需求

### 2.1 项目库系统
#### 2.1.1 左侧折叠式项目树 (新功能)
- 定位：结构管理与快速导航。
- 默认状态：折叠。界面左侧仅保留一个窄条或汉堡按钮，保持"禅意"留白。
- 展开状态：抽屉式滑出。显示紧凑的树形文件结构。
- 功能：
    - 快速查看所有项目结构。
    - 支持拖拽调整父子关系（如将任务A从项目1移到项目2）。
    - 快速添加：在树中快速录入新项目/任务。
    - 直接拖拽：可直接从左侧树拖拽任务到右下角的日历

#### 2.1.2 项目卡片网格视图

**功能描述**：
- 定位：浏览与直觉选择
- 以卡片形式展示所有根项目
- 支持三种视图模式：**紧凑、舒适、宽松**（动态调整卡片大小）
- **卡片内容**：项目名称、分类标签、项目进度、下一个待做任务提示
- **卡片设计**：简洁、**不显示时长数字**、浅色提示待做任务

**交互行为**：
- 点击卡片 → 进入项目详情视图，可以层层点击查看每一层子任务
- 拖拽卡片到日历 → 安排整个项目（快捷方式）

#### 2.1.3 项目详情与任务树
- 层级结构：支持无限层级数据结构。
- UI限制：视觉上缩进展示最多 5层。超过5层建议点击进入子视图（Breadcrumb View），防止缩进过深导致界面挤压。
- 详情页：
    - 支持富文本描述（图片、链接、日志）。
    - 子任务列表支持折叠/展开。
    - 叶子任务标识：可执行的底层任务有明确视觉标识（如空心圆点），可被拖拽。

#### 2.1.4 项目分类与过滤

**分类系统**：
- 分类系统：自定义标签与颜色（如：#FF6B9D 创意, #4ECDC4 学习）。
- 过滤栏：顶部标签栏筛选，支持多选。
- 搜索：实时搜索项目名或任务名。
- 完成态处理：已完成项目默认归档，可切换"显示全部"。

### 2.2 时间流（日历系统）

#### 2.2.1 日历视图

**基础设计**：
- 水平滚动的**7天日历条**，可以无限滚动到过去和未来的日期
- **粗粒度**：只显示日期（月/日 + 星期），不显示具体时刻
- **今日高亮**：当天用特殊颜色背景突出显示

#### 2.2.2 任务安排

- 拖拽叶子任务：
    - 从项目库拖入日历 → 创建今日待办。
    - 日历中显示：任务名 + 所属项目色条。
- 拖拽项目卡片 (特殊逻辑)：
    - 含义："项目聚焦块"。
    - 当拖拽整个项目卡片到日历时，不展开所有子任务，而是创建一个"[项目名]"的日程块。
    - 点击该日程块，弹窗列出该项目下的可执行任务供临时勾选。
- 日历内交互：
    - 移动/复制：按住短按拖拽可移动，长按复制任务到另一天（没做完顺延的场景）。
    - 状态管理：点击日历任务 → 标记完成 / 编辑 / 移除（移除仅从日历消失，任务状态回退至"未开始"，保留在项目库中）。

---

## 3. 界面布局与设计

### 3.1 整体布局

采用 "左中下" T型布局，注重留白
区域	占比/尺寸	功能描述
左侧栏 (Sidebar)	默认折叠 (50px) / 展开 (250px)	全局导航、结构调整、快速录入
上中部 (Main)	剩余宽度 x 60% 高度	项目卡片网格、任务详情、筛选器
下部 (Timeline)	剩余宽度 x 40% 高度	水平滚动日历、任务安排区

### 3.2 顶部工具栏

**左侧**：
- 左侧：App Logo & Slogan (可隐藏)

**右侧**：
- 视图密度切换、全局搜索框、设置入口

### 3.3 交互动效原则
- 展开/折叠：左侧栏和任务树使用 ease-in-out 平滑过渡。
- 拖拽：拖拽对象半透明跟随，目标区域（日历格）轻微放大或高亮边框 (border-style: dashed)。

### 3.4 设计原则

- **禅意呼吸感**：柔和渐变背景、圆润卡片、大量留白
- **色彩系统**：每个项目独特色彩，用于视觉识别
- **动画细腻**：卡片浮入、拖拽反馈、平滑过渡
- **无时间压力**：所有地方都不显示时长数字

### 3.5 系统设置入口
- 需要有系统设置入口，可以自行选择位置，系统设置内容可以自行设计，后续我看完效果再修改

## 4. 技术架构与数据存储

### 4.1 技术选型建议
- 前端：React (Next.js) 或 Vue 3 —— 处理复杂拖拽 (dnd-kit / react-dnd) 和状态管理。
- 后端/BaaS：Supabase (推荐) 或 Firebase。
    - 利用 Supabase 的 PostgreSQL 处理递归数据和实时同步。
- 状态管理：Zustand / Pinia + React Query。
- 离线支持：结合 localStorage 实现本地优先，网络恢复后自动同步。

### 4.2 可扩展
- 数据库结构需要可靠，并且支持后续修改数据库结构，比如增加多用户登陆功能，后续的数据库需要支持多用户独立使用这个web app

### 4.3 国际化/本地化
- 需要能支持多语言，浏览器自己判断用什么语言

### 4.4 多端适配
- 虽然是web app，但是也要适配在手机和平板设备浏览器里打开

## 5. 视觉设计规范

### 5.1 默认色彩系统

**背景渐变**：
- 主背景：`linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%)`
- 今日高亮：`linear-gradient(135deg, #fff8f9 0%, #ffe8ed 100%)`

**预设色彩（示例）**：
- `#FF6B9D` - 粉红色
- `#4ECDC4` - 青绿色
- `#FFD93D` - 金黄色
- `#A8E6CF` - 薄荷绿
- `#C7CEEA` - 淡紫色

色彩系统出了默认的调色盘，还应该可以在系统设置中手动添加和选择，包括主题颜色，调色盘内容等


## 6. 用户体验要点

### 6.1 核心体验原则

- **无压迫感**：不显示任何时长数字、截止日期、进度百分比
- **流畅自然**：所有交互都有平滑过渡，无突兀感
- **视觉愉悦**：柔和配色、圆润设计、细腻动画
- **完全自由**：随时可以调整、取消、重新安排，没有任何限制
- Forgiveness (宽容性)：误删有 Toast 撤回，日历任务移除不等于删除原任务。

### 6.2 空状态 (Empty States)
日历空空：显示 📥 Emoji，文案："把想做的事拖进来，一件就好"。
项目库空空：显示 🌱 Emoji，文案："种下第一个项目的种子"。

### 6.3 交互细节

- 卡片hover时显示彩色边框，提示可交互
- 拖拽时鼠标变为抓手，增强反馈
- 日历格子hover时显示虚线边框，提示可放置
- 任务树折叠/展开有平滑动画
- 移除任务的×按钮只在hover时变红

## 7. AI Agent实现提示

### 7.1 开发优先级

**Phase 1 - 核心功能（MVP）**：
1. 项目卡片网格视图
2. 项目详情与任务树展示
3. 拖拽叶子任务到日历
4. 7天日历视图
5. 移除已安排任务

**Phase 2 - 增强功能**：
1. 分类系统与过滤
2. 搜索功能
3. 视图模式切换
4. 拖拽项目卡片到日历

## 🎯 AI Agent快速实现Prompt

**当AI Agent收到"实现步纹应用"的指令时，应该：**

1. **首先**：阅读本PRD文档，理解核心理念（反焦虑、无时间压力）
2. **数据结构**：创建支持无限嵌套的项目/任务数据结构
3. **UI组件**：
   - 实现项目卡片网格（支持3种视图模式）
   - 实现任务树递归渲染
   - 实现7天日历条
4. **核心交互**：
   - HTML5 拖拽API实现任务到日历的拖拽
   - 项目详情的展开/折叠
   - 分类过滤和搜索
5. **视觉设计**：
   - **绝对不要显示时长数字**
   - 使用柔和的色彩和动画
   - 每个项目有独特的颜色标识
6. **代码质量**：
   - 使用React Hooks
   - 用useMemo优化过滤性能
   - CSS变量管理颜色
   - 递归函数渲染任务树

**最重要的原则**：始终记住这是一个"反焦虑"的工具，任何会产生压力的设计元素（时长、deadline、百分比）都应该被移除。

---

*文档版本：v1.0*  
*最后更新：2026-01-31*