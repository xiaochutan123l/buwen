services:
  buwen:
    build: .
    container_name: buwen
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data  # 持久化数据目录
    restart: unless-stopped
    environment:
      - NODE_ENV=production
